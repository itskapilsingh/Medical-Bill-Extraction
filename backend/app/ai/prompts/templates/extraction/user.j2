Document id: {{ doc_id }} — {{ total_pages }} page(s).

Extract every billing episode in this document. Begin by calling `list_pages` to
locate the billing pages, then read them and return the structured records and
flags. Do not include cover, certification, lab, or prescription pages.
