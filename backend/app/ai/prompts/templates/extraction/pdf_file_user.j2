Document id: {{ doc_id }}.
Filename: {{ filename }}.

The PDF is attached. Extract every billing episode from the full document,
including scanned pages and image-only screenshots. Keep separate invoices,
statements, claims, bills, and account sections separated by their visible
`invoice_number`. Do not include cover, certification, lab, or prescription pages
unless needed only to identify a provider/account for billing rows.
